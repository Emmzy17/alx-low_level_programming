#include <stdio.h>
/**
 * main - Entry point
 * Description: 'Print all digits'
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

        for (i = 0; i < 10= '\0'; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
